---
# defaults file for dle-ownership-images
cluster_name: "{{ cluster }}"
dc: "{{ region }}"
os_project: admin
owner_project: DLE-Project1
dump_file_folder: '/var/content/orphan'
dump_file: "{{ dump_file_folder }}/{{ cluster_name }}"
dump_host: 'automation-controller-geonosis-dfw3.ole.redhat.com'
...
